name: prd-terraform-cicd

on:
  push:
    branches:
    - 'release/**'
    - 'release'
    
jobs:
  terraform-cicd:
    uses: ./.github/workflows/reusable.terraform-cicd.yml
    with:
      environment: prd
      is_apply: false